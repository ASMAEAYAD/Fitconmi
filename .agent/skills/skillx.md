# SkillX — Performance & Animation

## Rules  
- Use CSS transitions (not JS) for hover effects
- Lazy load all images below the fold
- Animate with opacity + transform only (GPU accelerated)
- No layout shifts (set width/height on images)
- Use loading="lazy" on all images
- Prefer Tailwind classes over custom CSS
